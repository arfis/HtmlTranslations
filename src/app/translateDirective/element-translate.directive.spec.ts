import { ElementTranslateDirective } from './element-translate.directive';

describe('ElementTranslateDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementTranslateDirective();
    expect(directive).toBeTruthy();
  });
});
